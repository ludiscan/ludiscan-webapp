version: 1
frontend:
    phases:
        preBuild:
            commands:
                - curl -fsSL https://bun.sh/install | bash -s "bun-v1.3.0"
                - source /root/.bashrc
                - bun install --frozen-lockfile
        build:
            commands:
                - bun run prebuild:prod
                - bun run build:prod
    artifacts:
        baseDirectory: .next
        files:
            - '**/*'
    cache:
        paths:
            - .next/cache/**/*
            - node_modules/**/*
