import styled from '@emotion/styled';

import type { FC } from 'react';

const Component: FC = () => {
  return (
    <div>
      <h1>Home Page</h1>
    </div>
  );
};

export const HomePage = styled(Component)``;
